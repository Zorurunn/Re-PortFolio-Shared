export const DownloadCv = (props) => {
  return (
    <button
      className={
        props.width +
        " " +
        "bg-black text-white rounded pr-2 pt-1 pl-2 pb-1 dark:bg-[#D1D5DB] dark:text-black"
      }
    >
      履歴書を保存
    </button>
  );
};
